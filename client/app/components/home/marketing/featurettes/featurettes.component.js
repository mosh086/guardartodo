import template from './featurettes.html';
import controller from './featurettes.controller';
import './featurettes.scss';

let featurettesComponent = {
  restrict: 'E',
  bindings: {},
  template,
  controller
};

export default featurettesComponent;
