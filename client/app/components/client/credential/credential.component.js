import template from './credential.html';
import controller from './credential.controller';
import './credential.scss';

let credentialComponent = {
  restrict: 'E',
  bindings: {},
  template,
  controller
};

export default credentialComponent;
